<%= form_for(schedule_line_item) do |f| %>
  <% if schedule_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_line_item.errors.count, "error") %> prohibited this schedule_line_item from being saved:</h2>

      <ul>
      <% schedule_line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.text_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :detail_schedule_id %>
    <%= f.text_field :detail_schedule_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
